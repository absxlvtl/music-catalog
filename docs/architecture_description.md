# 🧱 Опис Мікросервісної Архітектури Проєкту "Music Catalog"

Даний проєкт реалізований як мікросервісна архітектура, що складається з трьох основних компонентів, кожен з яких ізольований у власному контейнері (Docker, Lab 6).

## 1. Загальна Топологія Системи



Система функціонує за принципом єдиної точки входу (API Gateway), яка маршрутизує запити до спеціалізованих внутрішніх сервісів, що працюють у приватній Docker-мережі.

## 2. Компоненти та Їх Ролі (Bounded Contexts)

Кожен внутрішній сервіс відповідає за свій **Обмежений Контекст (Bounded Context)**, що є ключовим принципом **DDD (Lab 3)**.

| Компонент | Роль | Основна Логіка (Домен) | Внутрішній Порт |
| :--- | :--- | :--- | :--- |
| **`gateway-service`** | **API Gateway** | Єдина точка входу. Автентифікація, трасування (`X-Request-Id`), проксіювання запитів. | 8080 (Публічний) |
| **`catalog-service`** | **Catalog BC** | Управління **Треками**, Артистами, завантаження MP3-файлів. | 3000 |
| **`playlist-catalog-service`** | **Playlist BC** | Управління **Плейлистами** та їхнім складом. | 3001 |

## 3. Зв'язок та Взаємодія Сервісів

### 3.1. Спілкування з Клієнтом (Front-end)

Клієнт (React-додаток) **завжди** звертається тільки до **`gateway-service`** на порт **`8080`**.

* `GET /tracks/...` → перенаправляється на `catalog-service:3000`
* `GET /playlists/...` → перенаправляється на `playlist-catalog-service:3001`

### 3.2. Внутрішня Взаємодія

Сервіси спілкуються між собою мінімально, дотримуючись принципу ізоляції контекстів:

* **`playlist-catalog-service`** може викликати **`catalog-service`** (через його внутрішній URL у Docker-мережі), щоб отримати деталі треку за його ID.
* Кожен сервіс має власну базу даних (SQLite/PostgreSQL) і не має прямого доступу до БД іншого сервісу.

## 4. Використані Технології (Стек)

* **Бекенд:** Node.js, Express.
* **Контейнеризація:** Docker (керується через `docker-compose.yml`).
* **База Даних:** SQLite (локально, для простоти розробки).